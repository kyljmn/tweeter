<%= react_component("nav/index", { 
  home: root_path,
  notifications: notification_index_path,
  users: user_index_path,
  profile: user_path(current_user),
  signout: destroy_user_session_path,
  signin: new_user_session_path,
  register: new_user_registration_path,
  currentUser: current_user
}) %>
