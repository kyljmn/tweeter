<h1> Make a new twit </h1>
<%= form_with model: @twit, url: user_twits_path(current_user.id), local: true do |form| %>
  <% if @twit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@twit.errors.count, "error") %> prohibited
        this twit from being saved:
      </h2>
      <ul>
        <% @twit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', user_twits_path %>
