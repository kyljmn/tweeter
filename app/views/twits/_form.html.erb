<%= form_with model: @twit, url: url, local: true, class: "flex w-full pb-2 border-b-8 border-t" do |form| %>
    <% if @twit.errors.any? %>
      <div class="field">
        <h2>
          <%= pluralize(@twit.errors.count, "error") %> prohibited
          this twit from being saved:
        </h2>
        <ul>
          <% @twit.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  <i class="fas fa-user-circle fa-3x m-4" ></i>
  <div className="flex flex-col px-2">
    <%= react_component("mention/index", {users: fetch_new_users}, {prerender: true}) %>

    <div className="flex justify-between">
      <i className="far fa-image fa-lg my-5 cursor-pointer twitter-text"></i>
      <%= form.file_field :images, multiple: true %>
      <%= submit_tag "Tweet", class: "rounded-full bg-twitter text-white px-4 py-2 border-none font-bold cursor-pointer" %> 
    </div>  
  </div>
<% end %>