  <% if current_user.followings.exists?(user.id) %>
    <%= link_to "unfollow", unfollow_user_path(user), method: :delete, class: "button -follow" %>
  <% elsif current_user.id == user.id %>
  <% else %>
    <%= link_to "follow", follow_user_path(user), method: :post, class: "button -unfollow" %>
  <% end %>
